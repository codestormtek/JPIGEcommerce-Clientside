{
  "name": "jpig-solution",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"API,FRONTEND,ADMIN\" --prefix-colors \"yellow,cyan,magenta\" \"npm run dev --prefix Api\" \"npm run dev --prefix Frontend.WEB\" \"npm run dev --prefix Admin.Web\"",
    "dev:api": "npm run dev --prefix Api",
    "dev:frontend": "npm run dev --prefix Frontend.WEB",
    "dev:admin": "npm run dev --prefix Admin.Web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
