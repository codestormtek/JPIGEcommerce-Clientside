"use client"
import React from 'react'
import Widget from '@/components/widgets/Widget'

function FeatureDiscount() {
    return (
        <Widget
            placement="feature-promos"
            variant="feature-cards"
        />
    );
}

export default FeatureDiscount
